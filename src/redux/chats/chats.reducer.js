const INITIAL_STATE = {
  chats: [],
};

const chatsReducer = (state = INITIAL_STATE, action) => {
  // console.log(state);
  switch (action.type) {
    default:
      return state;
  }
};

export default chatsReducer;
